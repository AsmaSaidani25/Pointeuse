class ServiceStatus {
  bool? _running;

  // bool get running {
  //   return _running;
  // }

  set running(bool value) {
    _running = value;
  }

  /* ServiceStatus(bool running) {
    _running = running;
  }*/
}
